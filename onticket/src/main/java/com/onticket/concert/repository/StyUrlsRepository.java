package com.onticket.concert.repository;

import com.onticket.concert.domain.StyUrls;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StyUrlsRepository extends JpaRepository<StyUrls, Long> {
}
